name: compliance-dashboard
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7

  # dash packages
  - dash=1.10

  # general packages
  - numpy
  - pandas

  # needed for dash-auth
  - requests

  # development
  - black
  - pytest

  # pip packages
  - pip
  - pip:
    - dash-auth
prefix: C:\Users\Adam.16KDR.000\.conda\envs\compliance-dashboard

