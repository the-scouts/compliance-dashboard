name: compliance-dashboard
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9

  # dash packages
  - dash
  - flask

  # general packages
  - lxml
  - numpy
  - pandas
  - pyarrow
  - python-dateutil
  - redis-py
  - pydantic

  # development
  - black
  - pytest
  - toml
  - isort
  - mypy
  - ipython
#
#  # ci
#  - gunicorn

  # pip packages
  - pip
  - pip:
    - flask-caching
